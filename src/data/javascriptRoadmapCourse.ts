import type { CourseStage, FrontendLesson } from './frontendCourse';

const jsLesson = (number: number, title: string, goal: string, analogy: string, content: string, practice: string): FrontendLesson => ({
  id: `javascript-roadmap-${number}`,
  number,
  stage: 'JavaScript' as CourseStage,
  title,
  duration: '2 часа',
  goal,
  analogy,
  content: content + '\n\n## Как закрепить\n\n' + practice,
  practice,
});

export const javascriptRoadmapCourse = {
  slug: 'javascript-roadmap',
  title: 'JavaScript по дорожной карте: от массивов до проектов',
  description: 'Полный практический маршрут по прикреплённой дорожной карте: углубляем структуры данных, современный синтаксис, DOM, события, асинхронность, ошибки, модули, инструменты и реальные проекты.',
  totalHours: 32,
  lessons: [
    jsLesson(1, 'Массивы: создание, индексы и изменение данных', 'Научиться уверенно создавать массивы, читать элементы, изменять их и понимать индексы.', 'Массив — это ряд пронумерованных ячеек в шкафчике. Первый номер — 0, а не 1.', `# Массивы: фундамент коллекций

Массив хранит несколько значений в определённом порядке. Он подходит для списка учеников, товаров, задач или результатов.

\`\`\`js
const students = ['Амин', 'Лейла', 'Мурад'];
console.log(students[0]);
students[1] = 'Зара';
students.push('Руслан');
console.log(students.length);
\`\`\`

Индекс — это позиция элемента. Свойство length показывает длину, но последний индекс всегда равен length - 1.`, 'Создайте массив из пяти учебных целей. Выведите первую и последнюю цель, замените одну цель и добавьте шестую. Объясните, почему последний индекс равен длине минус один.'),
    jsLesson(2, 'Методы массивов: push, pop, shift, unshift, splice и slice', 'Понять, какие методы меняют исходный массив, а какие возвращают новый результат.', 'Одни методы переставляют вещи прямо в шкафу, другие делают копию нескольких полок.', `# Изменяющие и копирующие методы

push и pop работают с концом массива, shift и unshift — с началом. splice может удалить или вставить элементы внутри. slice создаёт копию диапазона и не меняет исходный массив.

\`\`\`js
const tasks = ['HTML', 'CSS', 'JS'];
tasks.push('Git');
const first = tasks.shift();
const copy = tasks.slice(0, 2);
tasks.splice(1, 0, 'DOM');
console.log(first, copy, tasks);
\`\`\`

Всегда проверяйте документацию метода: важно знать, возвращает ли он значение, индекс или новый массив.`, 'Сделайте менеджер списка покупок. Реализуйте добавление, удаление последнего товара, удаление товара по индексу и получение копии первых трёх товаров.'),
    jsLesson(3, 'Методы массивов: forEach, map, filter, find, some, every и reduce', 'Выбирать метод массива по задаче и писать выразительный код без лишних циклов.', 'Методы массива — это команда специалистов: один перебирает, другой фильтрует, третий считает общий итог.', `# Функциональная работа с массивами

forEach выполняет действие для каждого элемента. map создаёт новый массив преобразованных значений. filter оставляет подходящие элементы. find возвращает первый найденный элемент. some и every отвечают на вопросы, reduce собирает одно итоговое значение.

\`\`\`js
const prices = [100, 250, 80];
const withTax = prices.map(price => price * 1.2);
const expensive = prices.filter(price => price > 100);
const total = prices.reduce((sum, price) => sum + price, 0);
console.log(withTax, expensive, total);
\`\`\`

Не используйте forEach, если вам нужно получить новый массив: для этого создан map.`, 'Создайте массив оценок. Получите оценки выше 3, проверьте, все ли оценки положительные, найдите первую пятёрку и посчитайте средний балл через reduce.'),
    jsLesson(4, 'Итерация и сортировка массивов', 'Освоить for, for...of, forEach и безопасную сортировку чисел и объектов.', 'Итерация — это обход полок одну за другой, а сортировка — расстановка предметов по правилу.', `# Перебор и порядок

for удобен, когда нужен индекс. for...of читается естественно, когда нужен только элемент. forEach подходит для простого действия. sort изменяет массив и по умолчанию сравнивает значения как строки.

\`\`\`js
const scores = [10, 2, 30];
scores.sort((a, b) => a - b);
for (const score of scores) console.log(score);
\`\`\`

Для объектов задайте функцию сравнения: items.sort((a, b) => a.price - b.price). Для копии используйте [...items].sort(...).`, 'Отсортируйте товары по цене от меньшей к большей и по названию. Сделайте копию массива перед сортировкой и проверьте, что исходный порядок сохранился.'),
    jsLesson(5, 'Объекты: свойства, вложенность и методы Object', 'Создавать модели данных и безопасно читать их свойства.', 'Объект — это карточка с подписанными полями: у человека есть имя, возраст и навыки.', `# Объекты

Объект хранит пары ключ-значение. Доступ через точку удобен для известных ключей, квадратные скобки подходят, когда ключ хранится в переменной.

\`\`\`js
const user = {
  name: 'Малика',
  age: 19,
  skills: ['HTML', 'JS'],
  address: { city: 'Назрань' }
};
const field = 'name';
console.log(user[field], user.address.city);
console.log(Object.keys(user));
console.log(Object.values(user));
console.log(Object.entries(user));
\`\`\`

Проверяйте наличие свойства через Object.hasOwn или hasOwnProperty, особенно если данные пришли извне.`, 'Создайте объект профиля студента с вложенным адресом и массивом навыков. Выведите пары ключ-значение и безопасно проверьте наличие поля email.'),
    jsLesson(6, 'Деструктуризация объектов и массивов', 'Извлекать нужные данные коротко и понятно, задавая значения по умолчанию.', 'Деструктуризация — это распаковка посылки по наклейкам: сразу берём нужные предметы, не перебирая всё содержимое.', `# Destructuring

\`\`\`js
const student = { name: 'Иса', age: 20, city: 'Магас' };
const { name, city, course = 'JavaScript' } = student;
const [first, second] = ['HTML', 'CSS'];
console.log(name, city, course, first, second);
\`\`\`

Имена переменных должны совпадать с ключами объекта, если не задано переименование: const { name: studentName } = student.`, 'Перепишите функцию renderUser так, чтобы она принимала объект и сразу извлекала name, avatar и role. Добавьте значение по умолчанию для отсутствующего role.'),
    jsLesson(7, 'let, const, области видимости и шаблонные строки', 'Понять современное объявление переменных и безопасно строить строки.', 'Область видимости — это комната, где лежат переменные: из другой комнаты нельзя взять локальную вещь без передачи.', `# Современный базовый синтаксис

const запрещает переназначение ссылки, let разрешает изменение значения. Оба объявления имеют блочную область видимости. Шаблонные строки позволяют собирать текст с выражениями.

\`\`\`js
const name = 'Саид';
let points = 0;
points += 10;
const message = 'Студент: ' + name + ', баллы: ' + points;
console.log(message);
\`\`\`

Для сложного текста используйте шаблонные строки в реальном коде: "Студент: \${name}". В учебных примерах всегда сначала проверьте, какие значения реально подставляются.`, 'Создайте функцию приветствия, которая принимает имя, количество уроков и процент прогресса и возвращает красивое сообщение.'),
    jsLesson(8, 'Стрелочные функции, this и callback-функции', 'Разобраться в кратком синтаксисе функций и понять, почему this ведёт себя иначе.', 'Callback — это записка, которую оставляют исполнителю: «когда закончишь, выполни эту функцию».', `# Функции современного JavaScript

Стрелка удобна для короткого выражения: const double = number => number * 2. У неё нет собственного this: значение берётся из внешнего контекста. Обычная функция получает this иначе, поэтому не заменяйте функции механически.

\`\`\`js
const names = ['Алия', 'Муса'];
const greetings = names.map(name => 'Привет, ' + name);
function runLater(callback) { callback(); }
runLater(() => console.log('Готово'));
\`\`\`

Callback встречается в методах массива, событиях и асинхронных операциях.`, 'Напишите функции calculateTotal и formatStudent. Передайте callback в функцию runAfterMessage, которая сначала выводит сообщение, а затем вызывает callback.'),
    jsLesson(9, 'Spread, Rest и копирование данных', 'Копировать коллекции, объединять данные и принимать любое число аргументов.', 'Spread раскладывает содержимое коробки, Rest собирает несколько предметов в одну коробку.', `# Spread и Rest

Spread записывается как three dots перед массивом или объектом. Он создаёт поверхностную копию и помогает объединять данные. Rest собирает оставшиеся параметры функции.

\`\`\`js
const base = ['HTML', 'CSS'];
const full = [...base, 'JavaScript'];
const profile = { name: 'Лейла' };
const updated = { ...profile, level: 'beginner' };
function sum(...numbers) { return numbers.reduce((a, b) => a + b, 0); }
\`\`\`

Поверхностная копия не клонирует вложенный объект. Для сложных данных учитывайте ссылочную природу объектов.`, 'Сделайте функцию mergeSettings, которая объединяет настройки по умолчанию и пользовательские настройки. Проверьте, что исходные объекты не изменились.'),
    jsLesson(10, 'Promises: состояние операции и цепочки then/catch', 'Понять, как описывать результат будущей операции и обрабатывать успех и сбой.', 'Promise — это талон заказа: сейчас заказа ещё нет, но есть обещание сообщить, готов он или нет.', `# Промисы

Promise бывает pending, fulfilled или rejected. then обрабатывает успех, catch — ошибку, finally выполняется в любом случае.

\`\`\`js
const request = new Promise((resolve, reject) => {
  setTimeout(() => resolve('Данные загружены'), 500);
});
request.then(value => console.log(value)).catch(error => console.error(error));
\`\`\`

Не создавайте Promise без необходимости: многие API уже возвращают промис.`, 'Создайте Promise для проверки пароля через задержку. Для короткого и длинного пароля покажите разные результаты и обработайте ошибку через catch.'),
    jsLesson(11, 'async/await и последовательные асинхронные шаги', 'Писать асинхронный код читаемо и не терять ошибки.', 'async/await — это ожидание заказа без блокировки всей кофейни: программа ждёт только там, где результат действительно нужен.', `# Async/await

Функция с async всегда возвращает Promise. await приостанавливает выполнение этой функции до результата, но не блокирует браузер.

\`\`\`js
async function loadProfile() {
  const response = await fetch('/api/profile');
  if (!response.ok) throw new Error('Ошибка ответа');
  return response.json();
}
loadProfile().then(profile => console.log(profile));
\`\`\`

Для параллельных независимых запросов используйте Promise.all, а не ждите их по одному.`, 'Напишите async-функцию, которая загружает пользователя и его задачи. Сначала сделайте последовательный вариант, затем оптимизируйте независимые запросы через Promise.all.'),
    jsLesson(12, 'DOM: поиск и чтение элементов', 'Получать элементы через разные методы и понимать, когда какой селектор применять.', 'DOM — это карта здания: getElementById находит комнату по номеру, querySelector — по точному описанию.', `# Выбор элементов

Используйте getElementById для уникального id, querySelector для первого совпадения и querySelectorAll для коллекции. Старые getElementsByClassName и getElementsByTagName возвращают живые коллекции и встречаются в старом коде.

\`\`\`js
const title = document.getElementById('title');
const button = document.querySelector('.save');
const cards = document.querySelectorAll('.card');
cards.forEach(card => console.log(card.textContent));
\`\`\`

Всегда учитывайте, что селектор может ничего не найти и вернуть null или пустую коллекцию.`, 'Создайте HTML из трёх карточек и найдите их всеми тремя современными способами. Выведите количество найденных карточек и текст первой.'),
    jsLesson(13, 'DOM: текст, атрибуты, стили и создание элементов', 'Изменять интерфейс безопасно и создавать новые узлы из данных.', 'Манипуляция DOM — это перестановка мебели: можно заменить табличку, перекрасить стену или добавить новый стул.', `# Изменение страницы

textContent меняет текст безопасно. innerHTML разбирает HTML-строку, поэтому опасен с непроверенными данными. setAttribute и getAttribute работают с атрибутами, classList — с классами.

\`\`\`js
const item = document.createElement('li');
item.textContent = 'Новая задача';
item.classList.add('task');
document.querySelector('#tasks').append(item);
item.remove();
\`\`\`

Для пользовательского текста предпочитайте textContent, а не innerHTML.`, 'Сделайте список задач, который создаёт li через createElement. Добавьте класс, data-id, кнопку удаления и удаление элемента через remove.'),
    jsLesson(14, 'События и объект Event', 'Реагировать на click, input, keydown, submit, load и scroll.', 'Событие — это звонок: target говорит, кто позвонил, preventDefault отменяет стандартный сценарий.', `# Обработка событий

addEventListener не заменяет существующие обработчики и позволяет подписать несколько функций. Объект event содержит target, currentTarget, key и методы управления поведением.

\`\`\`js
button.addEventListener('click', event => {
  event.preventDefault();
  event.currentTarget.classList.toggle('active');
});
form.addEventListener('submit', event => {
  event.preventDefault();
});
\`\`\`

Разделяйте обработчик и бизнес-логику: обработчик получает событие, функция решает, что делать.`, 'Создайте форму поиска: реагируйте на submit, очищайте стандартную перезагрузку, показывайте введённый запрос и поддержите отправку Enter.'),
    jsLesson(15, 'Асинхронные запросы: callbacks, fetch и XMLHttpRequest', 'Понимать эволюцию асинхронности и получать данные из API.', 'Callback — старый телефонный звонок, Promise — талон с обещанием, async/await — удобное окно выдачи.', `# Работа с API

Callbacks были первым распространённым способом сообщить о завершении операции. В больших цепочках они превращались в Callback Hell. Promises и async/await сделали последовательность понятнее.

\`\`\`js
async function loadQuotes() {
  const response = await fetch('https://example.com/quotes');
  if (!response.ok) throw new Error('Сервер ответил ошибкой');
  const quotes = await response.json();
  return quotes;
}
\`\`\`

XMLHttpRequest важен для чтения старых проектов, но в новых интерфейсах чаще используется fetch.`, 'Подключите тестовый API цитат или пользователей. Покажите loading, данные, пустой результат и ошибку сети. Сделайте кнопку повторной загрузки.'),
    jsLesson(16, 'Ошибки, модули, Node.js, npm, Vite, ESLint и следующий шаг', 'Научиться строить поддерживаемый проект и понять, куда двигаться после чистого JavaScript.', 'Модуль — отдельная мастерская, npm — склад инструментов, ESLint — внимательный проверяющий, Vite — конвейер сборки.', `# От ошибки к проекту

try/catch обрабатывает синхронные ошибки и ошибки внутри await. Для Promise используйте catch или try/catch вокруг await. Модули разделяют код через export и import.

\`\`\`js
// math.js
export const double = value => value * 2;
// app.js
import { double } from './math.js';
console.log(double(5));
\`\`\`

Node.js запускает JavaScript вне браузера. npm устанавливает зависимости и запускает scripts. Vite собирает современное приложение, а ESLint ищет потенциальные ошибки и несогласованный стиль. После уверенной практики переходите к React, Vue или Angular, понимая компоненты, props и state.`, 'Соберите финальный проект из дорожной карты: To-Do List с localStorage, приложение с API и небольшую игру. Разделите код на модули, добавьте npm script, запустите ESLint и напишите README с инструкцией запуска.'),
  ],
};

export const javascriptRoadmapStages = [
  { name: 'Данные', hours: 12, lessons: '1–6', description: 'Массивы, объекты, методы и современный синтаксис', color: 'yellow' },
  { name: 'Браузер', hours: 10, lessons: '7–9', description: 'DOM, элементы и пользовательские события', color: 'blue' },
  { name: 'Асинхронность', hours: 6, lessons: '10–11, 15', description: 'Promises, async/await, fetch и состояния интерфейса', color: 'purple' },
  { name: 'Проекты', hours: 4, lessons: '12–14, 16', description: 'Ошибки, модули, инструменты и финальная практика', color: 'green' },
] as const;
